{
  "name": "urca",
  "version": "0.0.0",
  "description": "Universal Reduced Computer Architecture is both a protocol for the memory layouts and composition of different systems to compose a modern-day computer and an RUN-RAM emulator for such system",
  "main": "src/index.ts",
  "author": "pinkspaces",
  "license": "CC0",
  "dependencies": {
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "dev": "NODE_PATH=./src nodemon -w ./src/ -e env,ts --exec \"node -r ts-node/register ./src/index.ts\"",
    "build": "tsc",
    "prod": "NODE_PATH=./dist node ./dist/index.js"
  }
}
